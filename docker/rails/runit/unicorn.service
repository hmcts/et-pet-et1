#!/bin/sh
exec 2>&1
export HOME=/rails
export WEBUSER=moj
sh /etc/envvars
cd $HOME
chown -R moj:moj $HOME
exec chpst -u $WEBUSER /usr/local/bundle/bin/bundle exec unicorn -p 80 -c ./config/unicorn.rb >> /rails/log/unicorn.log