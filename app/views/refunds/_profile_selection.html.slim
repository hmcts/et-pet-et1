fieldset.profile_selection
  legend = t('.profile_type')
  = f.input :profile_type,
          as: :radio_buttons,
          label: false,
          required: true,
          item_wrapper_tag: :div,
          collection: ['claimant_direct_not_reimbursed', 'claimant_via_rep', 'claimant_eat'],
          input_html: { data: { behavior: 'profile_type' } }

